# Generated sqlc Guidelines

This child file adds only local rules for `internal/infra/persistence/postgres/sqlc`.

The repo has no external users yet, so clean architecture and current best practices beat compatibility shims or speculative legacy paths.

The `.go` files in this directory are generated by sqlc. Do not hand edit those generated files; update migrations or SQL query definitions, then run `make sqlc` from `backend/` and review the generated diff. This `AGENTS.md` file is the local guidance exception.
