// Package core defines the canonical provider abstraction and shared domain
// models for the meltica SDK. The types exported here form the stable
// protocol surface that all concrete exchange adapters must implement. The
// abstractions cover REST (spot + futures), WebSocket events, canonical topic
// helpers and utility primitives shared across adapters.
package core
